// Simple JS just for testing form submission
document.addEventListener("DOMContentLoaded", () => {
    const form = document.querySelector("form");

    form.addEventListener("submit", (event) => {
        alert("Form submitted successfully!");
    });
});
